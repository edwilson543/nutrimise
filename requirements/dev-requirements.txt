# Running the application
django-extensions==3.2.3
# Requirements for running the development server over https
Werkzeug==2.3.6
pyOpenSSL==23.2.0

# Linting / typing
black==23.1.0
django-stubs==4.2.2
djangorestframework-stubs==3.14.2
flake8==6.0.0
isort==5.12.0
mypy==1.3.0

# Git
pre-commit==3.1.1

# Pytest plugins for assisting local dev (but not used in CI)
pytest-random-order==1.1.0
pytest-repeat==0.9.1
